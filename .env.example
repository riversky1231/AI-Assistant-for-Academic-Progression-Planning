# Copy this file to .env and adjust the non-secret settings.
# Set LLM_API_KEY only in the process or user environment; never put it in .env.
LLM_ENABLED=false
LLM_BASE_URL=https://api.deepseek.com
LLM_MODEL=deepseek-flash
LLM_TIMEOUT_SECONDS=30
AGENT_MAX_ROUNDS=4
